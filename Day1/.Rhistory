# importing input
data <- read.table("input2.txt", sep="\n")
View(data)
result <- rep(NA,dim(data)[1])
1:(length(result)-2)
1:(length(result)-1)
# creating empty vector for saving results
result <- rep(NA,dim(data)[1])
for (i in 1:(length(result)-2)) {
result[i] <- sum(data$depth[i],data$depth[i+1],data$depth[i]+2)
}
result
i = 5
data$depth[i]
# renaming column
colnames(data) <- "depth"
data$depth[i]
for (i in 1:(length(result)-2)) {
result[i] <- sum(data$depth[i],data$depth[i+1],data$depth[i]+2)
}
result
dim(result)[1]
rep(NA,length(result))
result_final <- rep(NA,length(result))
result
for (i in 2:length(result_final)) {
# checking decrease
if (result[i-1] > result[i]) {result_final[i] <- "decreased"}
# checking increase
if (result[i-1] < result[i]) {result_final[i] <- "increased"}
# equality
if (result[i-1] == result[i]) {result_final[i] <- "no change"}
print(resultresult_final[i])
}
result_final <- rep(NA,length(result))
for (i in 2:length(result_final)) {
# checking decrease
if (result[i-1] > result[i]) {result_final[i] <- "decreased"}
# checking increase
if (result[i-1] < result[i]) {result_final[i] <- "increased"}
# equality
if (result[i-1] == result[i]) {result_final[i] <- "no change"}
print(resultresult_final[i])
}
result_final <- rep(NA,length(result))
for (i in 2:length(result_final)) {
# checking decrease
if (result[i-1] > result[i]) {result_final[i] <- "decreased"}
# checking increase
if (result[i-1] < result[i]) {result_final[i] <- "increased"}
# equality
if (result[i-1] == result[i]) {result_final[i] <- "no change"}
print(result_final[i])
}
for (i in 2:length(result_final)) {
# checking decrease
if (result[i-1] > result[i]) {result_final[i] <- "decreased"}
# checking increase
if (result[i-1] < result[i]) {result_final[i] <- "increased"}
# equality
if (result[i-1] == result[i]) {result_final[i] <- "no change"}
print(paste(i,result_final[i],sep="_"))
}
length(result_final)
length(result)
result
rm(list=ls())
# setting working directory
setwd("C:/Users/julia/Desktop/AdventofCode2021/Day1")
# importing input
data <- read.table("input2.txt", sep="\n")
# renaming column
colnames(data) <- "depth"
# creating empty vector for saving results
result <- rep(NA,dim(data)[1])
result
for (i in 1:(length(result)-2)) {
result[i] <- sum(data$depth[i],data$depth[i+1],data$depth[i]+2)
}
table(result)
is.na(result)
table(is.na(result))
# we need to remove the two missing Values from the data
na.omit(result)
# we need to remove the two missing Values from the data
table(is.na(na.omit(result)))
# we need to remove the two missing Values from the data
result <- na.omit(result)
result
result_final <- rep(NA,length(result))
for (i in 2:length(result)) {
# checking decrease
if (result[i-1] > result[i]) {result_final[i] <- "decreased"}
# checking increase
if (result[i-1] < result[i]) {result_final[i] <- "increased"}
# equality
if (result[i-1] == result[i]) {result_final[i] <- "no change"}
print(paste(i,result_final[i],sep="_"))
}
# getting table of results
table(result_final)
#test data
data <- c(199,200,208,210,200,207,240,269,260,263)
# renaming column
colnames(data) <- "depth"
# creating empty vector for saving results
result <- rep(NA,dim(data)[1])
#test data
data <- data.frame(c(199,200,208,210,200,207,240,269,260,263))
# renaming column
colnames(data) <- "depth"
# creating empty vector for saving results
result <- rep(NA,dim(data)[1])
# we only go until 1998 because that is the last value we can compute a 3 value sliding window for
for (i in 1:(length(result)-2)) {
result[i] <- sum(data$depth[i],data$depth[i+1],data$depth[i]+2)
}
# we need to remove the two missing Values from the data
result <- na.omit(result)
result_final <- rep(NA,length(result))
for (i in 2:length(result)) {
# checking decrease
if (result[i-1] > result[i]) {result_final[i] <- "decreased"}
# checking increase
if (result[i-1] < result[i]) {result_final[i] <- "increased"}
# equality
if (result[i-1] == result[i]) {result_final[i] <- "no change"}
# sophisticated debugger
print(paste(i,result_final[i],sep="_"))
}
# getting table of results
table(result_final)
rm(list=ls())
# setting working directory
setwd("C:/Users/julia/Desktop/AdventofCode2021/Day1")
#test data
data <- data.frame(c(199,200,208,210,200,207,240,269,260,263))
# renaming column
colnames(data) <- "depth"
# creating empty vector for saving results
result <- rep(NA,dim(data)[1])
# we only go until 1998 because that is the last value we can compute a 3 value sliding window for
for (i in 1:(length(result)-2)) {
result[i] <- sum(data$depth[i],data$depth[i+1],data$depth[i]+2)
# sophisticated debugger
print(paste(i,result[i],sep="_"))
}
i = 1
data$depth[i]
data$depth[i+1]
data$depth[i]+2
sum(data$depth[i],data$depth[i+1],data$depth[i]+2)
sum(data$depth[i],data$depth[i+1],data$depth[i+2])
for (i in 1:(length(result)-2)) {
result[i] <- sum(data$depth[i],data$depth[i+1],data$depth[i+2])
# sophisticated debugger
print(paste(i,result[i],sep="_"))
}
rm(list=ls())
# setting working directory
setwd("C:/Users/julia/Desktop/AdventofCode2021/Day1")
# importing input
data <- read.table("input2.txt", sep="\n")
# renaming column
colnames(data) <- "depth"
# creating empty vector for saving results
result <- rep(NA,dim(data)[1])
# we only go until 1998 because that is the last value we can compute a 3 value sliding window for
for (i in 1:(length(result)-2)) {
result[i] <- sum(data$depth[i],data$depth[i+1],data$depth[i+2])
# sophisticated debugger
print(paste(i,result[i],sep="_"))
}
# we need to remove the two missing Values from the data
result <- na.omit(result)
result_final <- rep(NA,length(result))
for (i in 2:length(result)) {
# checking decrease
if (result[i-1] > result[i]) {result_final[i] <- "decreased"}
# checking increase
if (result[i-1] < result[i]) {result_final[i] <- "increased"}
# equality
if (result[i-1] == result[i]) {result_final[i] <- "no change"}
# sophisticated debugger
print(paste(i,result_final[i],sep="_"))
}
# getting table of results
table(result_final)
